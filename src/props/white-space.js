const whiteSpace = {
  prop: 'white-space',
  propName: 'text',
  keywordValues: {
    separator: '-',
    values: {
      'nowrap': 'nowrap',
      'prewrap': 'pre-wrap',
      'preline': 'pre-line'
    }
  }
};

export default whiteSpace;