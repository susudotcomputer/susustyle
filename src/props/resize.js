const resize = {
  propName: 'resize',
  prop: 'resize',
  keywordValues: {
    separator: '-',
    values: {
      default: 'both',
      none: 'none',
      x: 'horizontal',
      y: 'vertical'
    }
  }
};

export default resize;